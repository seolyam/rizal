import { Fraunces, Inter, Roboto_Mono } from "next/font/google";

export const fraunces = Fraunces({
  variable: "--font-fraunces",
  subsets: ["latin"],
  display: "swap",
});

export const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

export const mono = Roboto_Mono({
  variable: "--font-cousin",
  subsets: ["latin"],
  display: "swap",
});
